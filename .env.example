# Copy this file to `.env.local` and fill in values
# Files named `*.local` are git-ignored by default.

# Groq API key used by dev server endpoints in `vite.config.ts`
GROQ_API_KEY=
# Optional: Slack Incoming Webhook for usage logs (no DB required)
SLACK_WEBHOOK_URL=
# Optional: restrict access to this email domain (e.g., nexocean.com)
# If set, /api/* will require an X-Recruiter-Email matching this domain
ALLOW_EMAIL_DOMAIN=

# Frontend hint for client-side validation (same value as ALLOW_EMAIL_DOMAIN)
VITE_ALLOW_EMAIL_DOMAIN=

# Optional: Neon Postgres URL for Netlify Functions (server-side only)
# Example: postgresql://user:pass@host/db?sslmode=require
NEON_DATABASE_URL=
